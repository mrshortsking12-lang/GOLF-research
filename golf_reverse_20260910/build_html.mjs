import fs from 'node:fs/promises';
import { marked } from 'marked';

const dir = new URL('.', import.meta.url);
const report = await fs.readFile(new URL('GOLF_reverse_DCF.md', dir), 'utf8');
const result = JSON.parse(await fs.readFile(new URL('results.json', dir), 'utf8'));
const source = 'https://www.sec.gov/Archives/edgar/data/1672013/000167201326000157/ex991-q22026.htm';
let body = marked.parse(report.replace(/^# .*\n/, ''));
const percent = n => (100 * n).toFixed(2) + '%';
const matrix = result.sensitivity_matrix;
const allRates = matrix.flatMap(row => row.growth_rates);
const lowest = Math.min(...allRates), highest = Math.max(...allRates);
const matrixRows = matrix.map(row => `<tr><th scope="row">${percent(row.wacc)}</th>${row.growth_rates.map((rate, i) => {
  const base = row.wacc === result.wacc && result.terminal_rates[i] === result.terminal_growth;
  const shade = 96 - 17 * (rate - lowest) / (highest - lowest);
  return `<td style="background:hsl(155 28% ${shade}%);color:#172a34;${base ? 'outline:3px solid #075d47;outline-offset:-3px;font-weight:800;' : ''}">${percent(rate)}${base ? '<br><small>Base case</small>' : ''}</td>`;
}).join('')}</tr>`).join('');
const sensitivitySection = `<h2>Two-variable sensitivity analysis</h2>
<p><strong>How much annual revenue growth is needed to justify $85.29?</strong> Each cell independently solves the same five-year reverse DCF using the WACC in its row and terminal growth in its column.</p>
<p>WACC is the required return on the business. Terminal growth is the annual revenue growth assumed after Year 5. All other operating assumptions remain fixed.</p>
<table><caption style="text-align:left;padding:12px;font-weight:650">Required five-year revenue CAGR. Columns show terminal growth.</caption><thead><tr><th scope="col">WACC</th>${result.terminal_rates.map(rate=>`<th scope="col">${percent(rate)}</th>`).join('')}</tr></thead><tbody>${matrixRows}</tbody></table>
<p class="note">Darker cells mean more demanding growth. The outlined cell is the original 8.5% WACC / 2.5% terminal-growth case. Figures are dated September 10, 2026 and are not live market data.</p>
<p class="callout"><strong>Base case: ${percent(result.growth)} annual growth.</strong> Across these assumptions, required growth ranges from <strong>${percent(lowest)}</strong> to <strong>${percent(highest)}</strong>. This is an assumption range, not a statistical confidence interval.</p>
<ul><li><strong>Higher WACC raises required growth:</strong> future cash flows are worth less today.</li><li><strong>Higher terminal growth lowers required five-year growth in this model:</strong> the value of later cash flows increases.</li><li><strong>Terminal reinvestment also changes:</strong> each scenario recalculates Year 6 revenue, working capital and ERP investment before calculating terminal value.</li></ul>
<p>For comparison, actual revenue grew 13.8% in Q2 2026 and 10.5% in the first half. Those short-period growth rates do not establish sustainable five-year growth. The 13.5% terminal operating margin and other investment assumptions remain unchanged across this table.</p>
<p class="note">Validation: all 25 solved cases reproduce $85.29 within $0.00000001 per share. To reproduce a case in Excel, enter its WACC in C20 and terminal growth in C21, then Goal Seek C49 to zero by changing C18. Restore 8.5% and 2.5% and re-solve to return to the base case.</p>`;
body = body.replace(/<h2>WACC sensitivity<\/h2>[\s\S]*?(?=<h2>)/, sensitivitySection);
const headings = [];
body = body.replace(/<h2>(.*?)<\/h2>/g, (_, title) => {
  const id = title.toLowerCase().replace(/[^a-z0-9]+/g, '-');
  headings.push({id, title});
  return `<h2 id="${id}">${title}</h2>`;
});
body = body.replace(/<table>/g, '<div class="table-scroll" tabindex="0" role="region" aria-label="Financial data table"><table>').replace(/<\/table>/g, '</table></div>');
const growth = (result.growth * 100).toFixed(2);
const html = `<!doctype html>
<html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width, initial-scale=1">
<title>GOLF — Full reverse DCF and actual growth</title>
<meta name="description" content="Acushnet reverse discounted cash flow, reported growth, historical 10-K figures, five-year forecast, valuation and sources. September 10, 2026.">
<style>
:root{color-scheme:light;--ink:#172a34;--muted:#526673;--green:#075d47;--blue:#245b9c;--line:#d8e1e7;--paper:#fff;--bg:#eef3f6}
*{box-sizing:border-box}html{scroll-behavior:smooth;scroll-padding-top:24px}body{margin:0;color:var(--ink);background:var(--bg);font:17px/1.65 system-ui,-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif}a{color:var(--blue);text-underline-offset:3px}a:hover{color:var(--green)}a:focus-visible,button:focus-visible,[tabindex]:focus-visible{outline:3px solid #d68a00;outline-offset:4px}header{background:var(--ink);color:white;padding:38px max(24px,calc((100vw - 1180px)/2)) 34px}.eyebrow{font-size:14px;letter-spacing:.13em;text-transform:uppercase;color:#b9d4d1}h1{font-size:clamp(30px,4vw,46px);line-height:1.15;margin:12px 0}header p{margin:12px 0 0;color:#d2dfe6;max-width:800px}.toolbar{display:flex;justify-content:space-between;gap:20px;align-items:center}.toolbar button{background:transparent;border:1px solid #8099a7;color:white;padding:10px 15px;border-radius:5px;font:inherit;cursor:pointer}.layout{max-width:1230px;margin:auto;padding:32px 24px 70px;display:grid;grid-template-columns:210px minmax(0,1fr);gap:32px}aside nav{position:sticky;top:24px}nav strong{font-size:14px;text-transform:uppercase;letter-spacing:.08em}nav a{display:block;font-size:14px;line-height:1.4;padding:8px 0;text-decoration:none;color:var(--muted)}main{min-width:0}.panel,article{background:white;border:1px solid var(--line);border-radius:8px;padding:28px;margin-bottom:24px}h2{font-size:25px;line-height:1.3;margin:42px 0 18px;padding-top:16px;border-top:2px solid var(--line)}.panel h2{margin:0 0 14px;padding:0;border:0}p{margin:14px 0}small,.note{font-size:14px;color:var(--muted)}.cards{display:grid;grid-template-columns:repeat(3,1fr);gap:14px;margin:22px 0}.card{border-top:4px solid var(--blue);background:#f1f6fb;padding:18px 16px}.card.implied{border-color:var(--green);background:#edf6f2}.card .label{font-size:14px;font-weight:650;display:block}.card .number{font-size:36px;line-height:1.3;font-weight:750;display:block;margin:6px 0}.card small{display:block}.callout{border-left:4px solid var(--green);padding:12px 18px;background:#edf6f2}.warning{border-left-color:#a16b13;background:#fff7e9}.table-scroll{overflow:auto;margin:20px 0;border:1px solid var(--line);border-radius:5px}table{border-collapse:collapse;width:100%;font-size:15px;font-variant-numeric:tabular-nums}th{background:var(--ink);color:white;text-align:left;font-weight:600}th,td{padding:11px 13px;vertical-align:top;border-bottom:1px solid var(--line)}th:not(:first-child),td:not(:first-child){text-align:right;white-space:nowrap}tr:nth-child(even) td{background:#f5f8fa}tbody tr:last-child td{border-bottom:0}td:first-child{min-width:170px}code{font-size:14px;background:#edf2f5;padding:3px 6px;border-radius:3px;overflow-wrap:anywhere}.bar-row{display:grid;grid-template-columns:130px 1fr 66px;gap:12px;align-items:center;margin:14px 0;font-size:14px}.track{background:#e9eff3;height:14px;border-radius:3px}.bar{height:100%;background:var(--blue);border-radius:3px}.bar.required{background:var(--green)}.equation{font-variant-numeric:tabular-nums;background:#f4f7f9;padding:14px 18px;font-size:16px}.sources li{margin:10px 0}.sources{padding-left:22px}footer{font-size:14px;color:var(--muted);padding-top:15px}article>p:first-child{font-size:14px;color:var(--muted)}
@media(max-width:950px){.layout{grid-template-columns:1fr;gap:10px}aside nav{position:static;display:flex;gap:8px 18px;flex-wrap:wrap;margin-bottom:18px}nav strong{width:100%}nav a{padding:2px 0}header{padding:28px 24px}.panel,article{padding:22px}}
@media(max-width:600px){body{font-size:16px}.layout{padding:20px 12px}.cards{grid-template-columns:1fr}.card{padding:14px 18px}.card .number{font-size:32px}.toolbar{align-items:flex-start}.toolbar button{font-size:14px}.bar-row{grid-template-columns:110px 1fr 55px;gap:8px}h2{font-size:22px}.panel,article{padding:18px}th,td{padding:9px 11px}}
@media print{body{background:white;font-size:10pt}header{background:white;color:black;padding:0 0 15px;border-bottom:2px solid black}header p,.eyebrow{color:#333}h1{font-size:25pt}.layout{display:block;padding:15px 0;max-width:none}aside,.toolbar button{display:none}.panel,article{border:0;padding:0;margin:0 0 22px}.cards{grid-template-columns:repeat(3,1fr)}h2{font-size:16pt;break-after:avoid}.table-scroll{overflow:visible}table{font-size:9pt}th,td{padding:6px}th{background:#eee;color:black}tr,.card,.callout{break-inside:avoid}a{color:inherit}footer{font-size:9pt}}
</style></head><body>
<header><div class="toolbar"><div class="eyebrow">Acushnet Holdings Corp. · NYSE: GOLF</div><button type="button" onclick="window.print()">Print / Save PDF</button></div>
<h1>Reverse DCF & actual growth</h1><p>Full valuation analysis, grounded in your downloaded 2025 10-K and supplemented by the Q2 2026 SEC filings. Valuation date: September 10, 2026.</p></header>
<div class="layout"><aside><nav aria-label="Report sections"><strong>In this report</strong><a href="#actual-growth">Actual vs. required growth</a>${headings.map(h=>`<a href="#${h.id}">${h.title}</a>`).join('')}<a href="#sources">Source documents</a></nav></aside>
<main><section class="panel" aria-labelledby="disclaimer"><h2 id="disclaimer">AI and financial-advice disclaimer</h2><p class="callout warning"><strong>This report was created using artificial intelligence (AI) for educational and informational purposes only. It is not financial or investment advice and should not be used as such.</strong> It is not a recommendation to buy, sell, or hold any security. AI-generated content may contain errors, and the results depend on assumptions that may not hold. Verify the data and calculations independently and consult a qualified financial adviser before making investment decisions.</p></section><section class="panel" aria-labelledby="actual-growth"><h2 id="actual-growth">What is GOLF’s current growth?</h2>
<p><strong>GOLF’s latest reported quarterly revenue growth is 13.8%.</strong> Your reverse DCF solves for a different measure: the annual growth needed over five years to support the $85.29 share price.</p>
<div class="cards"><div class="card"><span class="label">Latest quarter · actual</span><span class="number">13.8%</span><small>Q2 2026 versus Q2 2025</small></div><div class="card"><span class="label">First half · actual</span><span class="number">10.5%</span><small>H1 2026 versus H1 2025</small></div><div class="card implied"><span class="label">Five-year CAGR · required</span><span class="number">${growth}%</span><small>Implied by the reverse DCF assumptions</small></div></div>
<div class="table-scroll" tabindex="0" role="region" aria-label="Reported revenue growth"><table><thead><tr><th>Period</th><th>Prior-year sales</th><th>Current sales</th><th>Reported growth</th><th>Constant currency</th></tr></thead><tbody><tr><td>Q2 2026 vs. Q2 2025</td><td>$720.5m</td><td>$820.0m</td><td>13.8%</td><td>14.2%</td></tr><tr><td>H1 2026 vs. H1 2025</td><td>$1,423.8m</td><td>$1,572.9m</td><td>10.5%</td><td>9.5%</td></tr></tbody></table></div>
<p class="equation">Quarterly revenue growth = ($820.0m ÷ $720.5m − 1) × 100 = <strong>13.8%</strong></p>
<p class="note">Reported growth includes currency effects. Constant-currency growth removes those effects. Source: <a href="${source}">August 6, 2026 earnings release</a>. Financial figures are rounded.</p>
<div aria-label="Growth comparison">
${[['2025 actual',4.1,false],['H1 2026 actual',10.5,false],['Q2 2026 actual',13.8,false],['Required CAGR',Number(growth),true]].map(([label,v,required])=>`<div class="bar-row"><span>${label}</span><div class="track"><div class="bar ${required?'required':''}" style="width:${v/16*100}%"></div></div><strong>${v}%</strong></div>`).join('')}
</div><p class="callout"><strong>The time horizon matters.</strong> One quarter of 13.8% year-over-year growth does not establish that GOLF can sustain ${growth}% growth annually for five years. The model also assumes improving operating margins.</p>
<p>Management said an earlier-than-usual GTS product launch benefited Q2 sales. First-half growth provides a broader recent measure. Full-year 2026 revenue guidance implies about 4.1% growth at the midpoint versus 2025; that figure is guidance, not a completed-year result. <a href="${source}">Company results and outlook</a>.</p>
</section><article aria-label="Full reverse discounted cash flow">${body}</article>
<section class="panel" aria-labelledby="sources"><h2 id="sources">Source documents</h2><ul class="sources"><li><a href="../../GOLF/golf%2010k%20HTML.html">Your downloaded 2025 10-K</a> — historical revenue, operating income, cash flows, investment and taxes.</li><li><a href="https://www.sec.gov/Archives/edgar/data/1672013/000167201326000057/golf-20251231.htm">2025 10-K on SEC.gov</a> — online copy of the historical filing.</li><li><a href="${source}">Q2 2026 earnings release on SEC.gov</a> — reported growth, latest guidance and launch timing.</li><li><a href="https://www.sec.gov/Archives/edgar/data/1672013/000167201326000158/golf-20260630.htm">Q2 2026 10-Q on SEC.gov</a> — balance sheet, debt, share data and investment outlook.</li><li><a href="https://stockanalysis.com/stocks/golf/history/">GOLF historical share prices</a> — September 10 price of $85.29.</li></ul><p><a href="GOLF_reverse_DCF.xlsx">Open the editable Excel model</a> · <a href="GOLF_reverse_DCF.md">Read the Markdown report</a></p></section>
<footer>This report opens without an internet connection. Online source links require internet access. The Excel and downloaded 10-K links use files in the original workspace.</footer>
</main></div></body></html>`;
for(const text of ['13.8%','10.5%','14.43%','5,223.1','84.57%','Source documents','created using artificial intelligence (AI)','not financial or investment advice','id="two-variable-sensitivity-analysis"','Base case']) {
  if(!html.includes(text)) throw new Error(`Missing report content: ${text}`);
}
await fs.writeFile(new URL('GOLF_reverse_DCF.html', dir),html);
console.log('Created GOLF_reverse_DCF.html with full model, actual growth and sources.');
